/*De modo geral, função é um "subprograma" que pode ser chamado por código externo (ou interno no caso de recursão) à função. Assim como o programa em si, uma função é composta por uma sequência de instruções chamada corpo da função. Valores podem ser passados para uma função e ela vai retornar um valor.
*/

function nome_da_funcao(/*Aqui são passados os parametros caso necessário*/){ 
    //é necessário usar () apos o nome da função, mesmo que não tenham parametros

    //Bloco de comandos da função
  
    console.log("Oi")
}

nome_da_funcao()

// funções são um assunto bem amplo, existem outros tipos de funções e até formas de programar(paradigma funcional) que é totalmente baseado em funções.